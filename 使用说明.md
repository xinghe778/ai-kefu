# AI客服系统一键安装脚本使用说明

## 脚本概览

为您的 ai-kefu 项目创建了完整的一键安装解决方案，包含以下脚本：

### 📦 脚本列表

| 脚本名称 | 功能描述 | 适用场景 |
|---------|---------|---------|
| `install.sh` | 完整安装脚本 | 新环境，完整 LAMP 部署 |
| `install-quick.sh` | 快速安装脚本 | 已有 LAMP 环境 |
| `verify.sh` | 安装验证脚本 | 检查安装结果 |
| `troubleshoot.sh` | 故障排除脚本 | 诊断和修复问题 |
| `README_INSTALL.md` | 详细安装指南 | 安装说明文档 |

## 🚀 快速使用

### 方法一：使用 curl 直接安装（推荐）

```bash
# 完整安装（新环境）
curl -sSL https://raw.githubusercontent.com/xinghe778/ai-kefu/main/install.sh | bash

# 快速安装（已有 LAMP）
curl -sSL https://raw.githubusercontent.com/xinghe778/ai-kefu/main/install-quick.sh | bash

# 验证安装
curl -sSL https://raw.githubusercontent.com/xinghe778/ai-kefu/main/verify.sh | bash

# 故障排除
curl -sSL https://raw.githubusercontent.com/xinghe778/ai-kefu/main/troubleshoot.sh | bash
```

### 方法二：下载脚本到本地

```bash
# 下载所有脚本
wget https://raw.githubusercontent.com/xinghe778/ai-kefu/main/install.sh
wget https://raw.githubusercontent.com/xinghe778/ai-kefu/main/install-quick.sh
wget https://raw.githubusercontent.com/xinghe778/ai-kefu/main/verify.sh
wget https://raw.githubusercontent.com/xinghe778/ai-kefu/main/troubleshoot.sh

# 添加执行权限
chmod +x install.sh install-quick.sh verify.sh troubleshoot.sh

# 运行安装
sudo ./install.sh          # 完整安装
sudo ./install-quick.sh    # 快速安装
./verify.sh               # 验证安装
sudo ./troubleshoot.sh     # 故障排除
```

## 📋 使用场景对比

| 安装类型 | 优点 | 缺点 | 适用情况 |
|---------|------|------|---------|
| **完整安装** | 自动配置所有依赖，一键完成 | 安装时间长（5-15分钟） | 全新服务器，新用户 |
| **快速安装** | 安装速度快（1-3分钟） | 需要手动安装 LAMP | 已配置 LAMP 环境 |
| **手动安装** | 完全自定义配置 | 需要技术知识 | 专业用户，特殊需求 |

## 🔧 详细使用说明

### 1. 完整安装 (install.sh)

适用于全新环境，自动配置所有组件：

```bash
# 标准安装
sudo ./install.sh

# 安装日志会保存到
/var/log/ai-kefu-complete-install.log
```

**安装流程**:
1. ✅ 系统兼容性检查
2. ✅ 安装系统依赖 (wget, curl, git, etc.)
3. ✅ 安装并配置 MySQL 数据库
4. ✅ 安装并配置 Apache Web 服务器
5. ✅ 安装并配置 PHP 运行环境
6. ✅ 安装 Composer 包管理器
7. ✅ 从 GitHub 克隆项目代码
8. ✅ 创建和配置数据库
9. ✅ 初始化项目设置
10. ✅ 配置 Apache 虚拟主机
11. ✅ 设置文件权限
12. ✅ 验证安装结果

### 2. 快速安装 (install-quick.sh)

适用于已配置 LAMP 环境的服务器：

```bash
# 快速安装
sudo ./install-quick.sh

# 安装日志会保存到
/var/log/ai-kefu-quick-install.log
```

**安装流程**:
1. ✅ 环境检查 (PHP, MySQL, Apache)
2. ✅ 从 GitHub 克隆项目
3. ✅ 创建数据库和用户
4. ✅ 配置虚拟主机
5. ✅ 设置权限
6. ✅ 快速验证

### 3. 验证安装 (verify.sh)

检查安装是否成功：

```bash
# 运行验证
./verify.sh

# 验证内容
- Apache 服务状态
- MySQL 服务状态
- 项目文件完整性
- PHP 环境检查
- 数据库连接测试
- Web 访问测试
```

### 4. 故障排除 (troubleshoot.sh)

自动诊断和修复常见问题：

```bash
# 运行故障排除（需要 root 权限）
sudo ./troubleshoot.sh

# 功能包括
- 自动检测问题
- 修复服务状态
- 修复权限问题
- 检查配置语法
- 生成诊断报告
- 提供修复建议
```

## 📁 文件结构

下载的脚本应该放在同一目录下：

```
ai-kefu-install/
├── install.sh              # 完整安装脚本 (437行)
├── install-quick.sh        # 快速安装脚本 (383行)
├── verify.sh              # 验证脚本 (129行)
├── troubleshoot.sh        # 故障排除脚本 (477行)
└── README_INSTALL.md      # 详细安装指南
```

## 🔒 安全考虑

### 文件权限
```bash
# 下载后设置正确的权限
chmod 700 install.sh
chmod 700 install-quick.sh
chmod 755 verify.sh
chmod 700 troubleshoot.sh
```

### Root 权限
- `install.sh`, `install-quick.sh`, `troubleshoot.sh` 需要 root 权限
- `verify.sh` 可以普通用户运行
- 脚本会检查权限并在权限不足时提示

### 网络要求
- 需要稳定的互联网连接
- 会从 GitHub 克隆项目: `https://github.com/xinghe778/ai-kefu.git`
- 会下载依赖软件包

## 📊 性能参数

| 操作 | 完整安装 | 快速安装 | 验证检查 |
|------|---------|---------|---------|
| **时间** | 5-15分钟 | 1-3分钟 | 30秒 |
| **内存** | 100-200MB | 50-100MB | 10-20MB |
| **磁盘空间** | 500MB-1GB | 50-100MB | 0 |
| **网络** | 100-200MB | 20-50MB | 1-5MB |

## 🛠️ 故障排除

### 常见问题

**Q: 提示 "Permission denied"**
```bash
# 给脚本添加执行权限
chmod +x install.sh
```

**Q: 提示 "需要root权限"**
```bash
# 使用 sudo 运行
sudo ./install.sh
```

**Q: 网络连接失败**
```bash
# 检查网络连接
curl -I https://github.com

# 如果在企业网络，可能需要代理设置
export http_proxy=http://proxy.company.com:8080
export https_proxy=http://proxy.company.com:8080
```

**Q: 安装后无法访问网站**
```bash
# 运行故障排除脚本
sudo ./troubleshoot.sh

# 检查服务状态
systemctl status httpd
systemctl status mysqld
```

### 获取帮助

1. **查看安装日志**:
   ```bash
   tail -f /var/log/ai-kefu-complete-install.log
   tail -f /var/log/ai-kefu-quick-install.log
   ```

2. **查看错误日志**:
   ```bash
   tail -f /var/log/httpd/ai-kefu-error.log
   ```

3. **运行诊断**:
   ```bash
   sudo ./troubleshoot.sh
   ```

4. **查看系统状态**:
   ```bash
   ./verify.sh
   ```

## 📞 技术支持

### 联系方式
- **GitHub Issues**: https://github.com/xinghe778/ai-kefu/issues
- **项目主页**: https://github.com/xinghe778/ai-kefu

### 提供信息
在寻求技术支持时，请提供：
- 操作系统版本: `cat /etc/centos-release`
- 脚本执行日志
- 错误信息截图
- 故障诊断报告

### 脚本特点
- ✅ 完全自动化安装
- ✅ 智能错误检测和恢复
- ✅ 详细的安装日志
- ✅ 彩色输出界面
- ✅ 安全权限设置
- ✅ 跨版本 CentOS 支持
- ✅ 完整故障排除方案

---

**选择适合您环境的安装方式，享受一键部署的便捷体验！**